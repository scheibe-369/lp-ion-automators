---
---

<div class="bg-primary py-3 overflow-hidden">
	<div class="relative flex">
		<div class="flex shrink-0 animate-marquee gap-8">
			{Array(10).fill(null).map(() => (
				<span class="text-white text-sm font-semibold uppercase tracking-widest whitespace-nowrap">
					ION + Automators Club — Turma Inaugural
					<span class="mx-4 opacity-50">✦</span>
				</span>
			))}
		</div>
		<div class="flex shrink-0 animate-marquee gap-8" aria-hidden="true">
			{Array(10).fill(null).map(() => (
				<span class="text-white text-sm font-semibold uppercase tracking-widest whitespace-nowrap">
					ION + Automators Club — Turma Inaugural
					<span class="mx-4 opacity-50">✦</span>
				</span>
			))}
		</div>
	</div>
</div>
