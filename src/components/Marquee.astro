---
---

<div class="w-full py-3 overflow-hidden border-y border-white/5 bg-white/5 backdrop-blur-md">
	<div class="relative flex">
		<div class="flex shrink-0 animate-marquee gap-8">
			{Array(10).fill(null).map(() => (
				<span class="text-white/80 text-sm font-semibold uppercase tracking-widest whitespace-nowrap">
					ION + Automators Club — Turma Inaugural
					<span class="mx-4 text-primary opacity-50">✦</span>
				</span>
			))}
		</div>
		<div class="flex shrink-0 animate-marquee gap-8" aria-hidden="true">
			{Array(10).fill(null).map(() => (
				<span class="text-white/80 text-sm font-semibold uppercase tracking-widest whitespace-nowrap">
					ION + Automators Club — Turma Inaugural
					<span class="mx-4 text-primary opacity-50">✦</span>
				</span>
			))}
		</div>
	</div>
</div>
